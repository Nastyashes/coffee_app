name: coffee_app

description: "My First Flutter app, for portfolio only"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_bloc: ^8.1.5
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  flutter_rating_bar: ^4.0.1
  bloc: ^8.1.4
  equatable: ^2.0.5
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.21
  path_provider: ^2.1.3
  path: ^1.9.0
  responsive_framework: ^0.1.9
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  latlong2: 0.9.1
  flutter_map: ^7.0.2
  html: ^0.15.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  drift_dev: ^2.18.0
  build_runner: ^2.4.9
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true
  assets:
    - assets/image/
    - assets/icons/
